nodeLinker: node-modules

# Bundled Yarn release so environments without Corepack (e.g. EAS Build workers,
# which ship Yarn 1) delegate to Yarn 4 instead of failing on the packageManager field.
yarnPath: .yarn/releases/yarn-4.12.0.cjs
