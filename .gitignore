node_modules
.pnp.*
.yarn/*
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions

dist
build
.turbo

# Generated native projects (Expo prebuild / expo run) — regenerated from app.json, not committed
apps/*/android
apps/*/ios

.env
.env.*
!.env.example

.expo

# Per-machine Claude Code settings. `.claude/launch.json` IS committed — it's the
# shared dev-server config the design-verification workflow in docs/DESIGN.md uses.
.claude/settings.local.json